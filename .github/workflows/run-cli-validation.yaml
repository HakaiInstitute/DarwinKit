name: Test CLI Validation

on:
  # push:
  #   branches:
  #     - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          token: ${{ secrets.CI_TOKEN }}
          submodules: recursive

      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x # Run with latest stable Deno.

      # Validate the darwin core archive
      - run: deno task cli validate --format markdown_summary_action --config ../../external/rocky-subtidal-fish-invertebrate_config.yaml

      # set github action summary
      - run: echo "$(cat packages/cli/validation_results/validation-results.md)" >> $GITHUB_STEP_SUMMARY
