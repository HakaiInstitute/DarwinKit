% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{dwk_validate}
\alias{dwk_validate}
\title{Validate a kit with the DarwinKit engine}
\usage{
dwk_validate(kit, stage_dir = NULL, bin = NULL, runner = NULL)
}
\arguments{
\item{kit}{A \code{dwk_kit} with at least one dataset.}

\item{stage_dir}{Optional directory to stage into (kept afterwards —
commit it to git or inspect the generated config). Defaults to a fresh
temporary directory.}

\item{bin}{Optional explicit path to the \code{dwkt} binary.}

\item{runner}{Subprocess runner, exposed for testing. A
\verb{function(bin, args)} returning \code{list(stdout, stderr, status)}.}
}
\value{
A \code{dwk_report}; see \code{\link[=dwk_is_valid]{dwk_is_valid()}} and \code{\link[=dwk_issues]{dwk_issues()}}.
}
\description{
Stages the kit as a shadow workspace (Parquet + \code{darwinkit.yaml}), runs
\verb{dwkt validate --format json}, and parses the result. The \code{dwkt} binary is
resolved from \code{bin}, then the \code{DARWINKIT_BIN} environment variable, then
the PATH; if none resolve, an error explains how to install it.
}
\examples{
\dontrun{
report <- dwk_init("survey") |>
  dwk_dataset("events", "Event", events_df) |>
  dwk_validate()
dwk_issues(report)
}
}
